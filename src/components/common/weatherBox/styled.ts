import styled from "styled-components";

export const WeatherBoxStyled = styled.div`
  text-align: center;
  margin: 40px 0;
`;

export const ImageStyled = styled.img`
  width: 60%;
`;

export const TemperatureStyled = styled.h4`
  position: relative;
  font-size: 64px;
  line-height: 1;
  font-weight: 700;
  margin: 20px 0 6;
`;

export const SpanTemperatureStyled = styled.span`
  position: absolute;
  font-size: 24px;
  margin-left: 4px;
  font-family: "Roboto", Sans-serif;
`;
